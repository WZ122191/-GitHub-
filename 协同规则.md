# 国赛工作区（D:\cumcm）· 项目公约（三壳开工必读）

> 开赛：09-10 18:00 → 09-13 16:00 提交。任何壳/任何新会话开工前先读本文件。
> 
> **目录已重组（2026-09-09）**：比赛活动全部在 `正式比赛\`；本文件中的路径均已更新。
> 
> **本区身份（2026-09-10）**：本工作区由 `D:\国赛\` 完整复制建立，作为三平台协同的正式比赛区；`D:\国赛\` 原区保留不动，两区各自独立演进，改动互不同步。钩子状态文件、MCP 存储路径均已按本区路径重配。

## 0. 三壳分工（一句话版）

| 壳                         | 模型                                 | 只干这些                                      |
| ------------------------- | ---------------------------------- | ----------------------------------------- |
| **Cursor**                | GPT-5.6 Sol（≤10次/天）+ Grok/Composer | 主力：建模推导、算法定生死、结果核验（Sol）；数据清洗、写码迭代调试（Grok） |
| **ZCode**                 | GLM-5.3（白天）/ Flash（夜间无限）           | 论文正文、文献提炼、算法论述；23:00–09:00 夜间自动迭代         |
| **DSH**（DeepSeek Harness） | DeepSeek V4 Pro                    | 写码兜底、批量写码、推演核验                            |
| **OpenCode**（备用壳）         | AMD×4 / 商汤 / Kimi                  | 免费打杂：排版、翻译、格式、日志；Kimi 网页端做深度检索            |

## 1. 任务路由表（写死，人按表发，禁止让模型自己挑模型）

| 任务类型             | 路由                        | 例外             |
| ---------------- | ------------------------- | -------------- |
| 排版 / LaTeX / 格式化 | L1：AMD#1→#2→#3→#4         | 全挂→智谱赠送包       |
| 翻译 / 文献总结 / 摘要初稿 | L2：智谱赠送包                  | 全挂→Kimi        |
| 检索 / 深度找资料       | Kimi 深度检索 + ZCode MCP     | 挂→aiapi        |
| 数据清洗 / 绘图 / 简单实现 | L1：AMD                    | 结果不对→DeepSeek  |
| **写码 / 迭代 / 推演** | M：DeepSeek（或 Cursor Grok） | 预算熔断→L1        |
| **论文正文 / 算法推导**  | P：ZCode GLM-5.3           | 全挂→DeepSeek    |
| **卡死 / 定生死**     | T：GPT-5.6 Sol             | ≤10 次/天，用一次记一次 |
| 结果核验             | DeepSeek 出结果 + 人裁判        | 打杂层产出一律不直接采信   |

接力顺序：L1(AMD免费) → L2(智谱赠送) → M(DeepSeek) → P(GLM) → T(Sol)。
切换信号：429 / 余额0 / 连挂2次 / 质量异常（升一层）/ 23:50 日结 / 预算熔断（降级）。
**红线**：任何信号出现后 5 分钟内完成切换+冒烟；切换只发生在任务边界，绝不在推理中途。

## 2. 常数约定（全队统一，写代码前看这里）

- 随机模型一律 `random_state=42`（或 `seed=42`），保证可复现；
- 图：PNG **300 dpi**；`plt.rcParams["font.sans-serif"]=["SimHei","Microsoft YaHei"]`、`plt.rcParams["axes.unicode_minus"]=False`；
- 产物落盘（2026-09-09 重组后）：比赛活动一律在 `正式比赛\`；题目工作区 `正式比赛\题目工作区\problem_X\`，最终产出 `正式比赛\03_产出\`；
- 交流与文档：中文；时间记本地时间，文件名日期 `MMDD`；
- 检索优先 Crossref（最稳），arXiv/OpenAlex 429 就退避换源；
- **预算红线**：DeepSeek 当日 >¥40 或总盘 >¥350 → 全部降级打杂免费层；
- 搜索优先级：Sci-Hub → 纬度互助 → 科研通 → AI 检索（Kimi 深度检索 / ZCode MCP）。

## 3. Key 存放位置（不写明文）

- 统一配置：`正式比赛\tools\keys.json`（本机文件，**严禁**上传/截图/贴日志），样例见同目录 `keys.example.json`；也支持环境变量同名注入；
- DeepSeek：DSH 配置内 + Cursor 自定义模型（备用）；GLM：ZCode 登录态（无需 key）；Kimi：网页端为主；
- 冒烟：`python 正式比赛\tools\smoke_all.py`（一键 ping 全通道，`--task` 加做算术验证），报告落 `正式比赛\02_checkpoint\冒烟记录\`。

## 4. 交接与接力（细节见 `正式比赛\01_交接箱\交接单_模板.md`；子进程切分用同目录 `子进程卡_模板.md`）

跨壳交接六步 = 落盘 → 重述 → 新会话 → 冒烟 → 记账 → 回滚。
每日 23:50–00:10 日结：抄额度表 → 定明日起点 key → 全链路冒烟 → 写排班。

## 5. 关键文件索引

**2026-09-09 重组**：一切比赛活动集中到 `正式比赛\`，根目录只留平台配置与资料库。

```
D:\cumcm\
├── README.md            ← 本文件（公约）
├── AGENTS.md            ← ZCode 工作区规则（含流水线规范第 8/9 条）
├── 正式比赛\            ← ★ 比赛活动全部在此
│   ├── 00_任务墙.md      ← 总调度板（唯一事实源）
│   ├── 01_交接箱\       ← 跨壳交接单 + 子进程卡模板
│   ├── 02_checkpoint\   ← 状态快照 / 冒烟记录 / 模板备份 / token_gauge.json
│   ├── 03_产出\         ← 最终产出（模板\cumcmthesis 在此）
│   ├── 04_决策日志.md    ← 选型与弃用记录（DEC-001…）
│   ├── 05_标准\         ← 检验标准 V1.2 / 坑-条款映射表 / 总规程副本
│   ├── tools\           ← paper_search / qc_gate / gauge / smoke_all / keys.json
│   ├── 题目工作区\       ← problem_0_热身 / problem_2024C；开赛新题建在此
│   ├── 做图\             ← 赛时绘图资产
│   └── 99_归档\         ← 旧缓存 / 源码 zip / 一次性脚本
├── library\             ← 数模资料库（见 math-model-library skill，勿动）
├── arxiv_papers\        ← arxiv MCP 下载目录（勿动）
└── .zcode\ .vscode\ .mimosa\   ← 平台配置（勿动）
```

---

# 附：环境与工具备忘（2026-09-09 实测）

## 1. Python 环境（Python 3.12，位于 D:\pyth）

清华 pip 源安装，可直接 import：numpy / scipy / pandas / matplotlib / seaborn / openpyxl / statsmodels / scikit-learn / networkx / cvxpy / pulp / sympy / arxiv / scholarly / requests / beautifulsoup4 / lxml。

## 2. MCP 服务器（.zcode\config.json，改动需重启会话生效）

| 名称                  | 用途                            | 状态  |
| ------------------- | ----------------------------- | --- |
| arxiv               | 论文搜索/下载，PDF 存 `arxiv_papers\` | ✅   |
| memory              | 跨会话记忆（模型选型、参数、坑）              | ✅   |
| sequential-thinking | 结构化多步推理                       | ✅   |

熔断器钩子 `.zcode\hooks\circuit_breaker.py`：同一网络源 10 分钟内失败 3 次 → 自动熔断拒绝调用并提示换源，10 分钟后解封。

## 3. 论文检索

```
python 正式比赛\tools\paper_search.py "关键词" --source arxiv,openalex,crossref,semantic --max 5
```

免 API key，结果存脚本同目录 `正式比赛\tools\last_search.json`。Crossref 最稳；arXiv/OpenAlex/SemanticScholar 偶发 429（已内置退避）。

## 4. 论文模板

`正式比赛\03_产出\模板\cumcmthesis\`：`cumcmthesis.cls` + `cumcm2026.sty` + `example.tex`（已实测 XeLaTeX 编译出 12 页 PDF）。备份在 `正式比赛\02_checkpoint\模板备份_cumcmthesis_0909\`。用法详见 ZCode skill `cumcm-thesis-latex`。

## 5. Markdown 查看/编辑（MarkText）

- 程序：`E:\markdown reader\MarkText\MarkText.exe`（0.17.1），`.md` 双击/系统默认打开方式已指向它（用户级注册表 `HKCU\Software\Classes`，2026-09-09 实测通过）。
- **重要**：该版本 Electron 较老，在本机 RTX 5070 上 GPU 加速会**随机启动崩溃**，注册表关联里已固定加 `--disable-gpu`（软渲染，稳定）。日后若升级 MarkText 新版，可去掉该参数：把 `HKCU\Software\Classes\Markdown\shell\open\command` 和 `HKCU\Software\Classes\MarkText.md\shell\open\command` 里的 `--disable-gpu ` 删掉即可。
