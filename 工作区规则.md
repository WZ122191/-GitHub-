# 工作区规则（国赛）

> 竞赛工作区 `D:\cumcm`。**本文件每次请求全量注入，只放"必须记住的规则 + 去哪查"，细则一律不写在这里。**
> 三份真源：题面 `题目工作区/problem_2026C/02_analysis/题目详解.md`｜口径 `…/口径冻结表_Q1_20260911.md`｜流程 `05_标准/AI_长任务流水线检验标准.md`（现行版，修订史见文内）。
> **两处索引**：`05_标准/README.md`（读什么）｜`01_交接箱/README.md`（**各问口径/指令/模板的现行状态与归属，含三张专项分派模板**）。新增文件必须入 `01_交接箱/README.md` 否则视为未交付。
> **新增规则写进 `05_标准/`，再在此处留一行指针**（见第 13 条）。

1. **语言**：与用户交流、写文档一律中文。
2. **产物落盘**：代码/图表/结果一律落 `正式比赛/`；按题落 `题目工作区/problem_<题号>/`（子目录 00_problem…07_references）；工作区根只留平台配置（AGENTS/.cursor/.zcode/.vscode）、`library`、`arxiv_papers`、README。
3. **论文检索**：`python 正式比赛/tools/lit_hunt.py "关键词" -n 10 --download 3`，**源必须显式给**（crossref/arxiv/europepmc/core/doaj 可用；**openalex、semantic 已失效**，返 0）。`paper-search download <源> <paper_id> -o <目录>`（`<源>` 必填位置参数）。**DOI 只能取自 API 返回元数据、禁止凭记忆写出**；下载不出但 DOI 为真的进 `04_文献/DOI_待代下清单.md`；**核验全文前不得引用其内容**。源实测表、死链、限流对策见 `05_标准/检索作业卡.md`。
4. **可复现**：每个结果都要有能跑通的脚本；随机模型固定 `random_state`（或 `seed=42`）。
5. **图表规范**：`plt.rcParams["font.sans-serif"] = ["SimHei", "Microsoft YaHei"]`、`axes.unicode_minus = False`；存 PNG 300 dpi。
6. **MCP**：arxiv / memory / sequential-thinking / paper-search / exa（key 见 `tools/keys.json`）；重要阶段性结论（模型选型/参数/坑）写 memory，便于跨会话延续。
7. **限流对策**：共享出口遇 429 先退避重试，再换数据源，不卡死单源。
8. **流水线规范**：开场引《`05_标准/AI_长任务流水线检验标准.md`》（现行版本见该文件头部版本行）；"完成"声明前过对应工位检验表（自动项 `python 正式比赛/tools/qc_gate.py`，⭐ 不允许跳过）；外部数据字符串进门先 strip 且在筛选前；**给人看的 CSV 一律 `encoding="utf-8-sig"`（Q-36）**；**跨问引用先过 Q-37 三分法（见第 14 条）**；写文件/模板后必须回读；优化/预测结果独立口径复核；B/C 判定与踩坑记决策日志。**决策日志分片制**：各壳只写自己的 **`正式比赛/04_决策/dec_<YYYYMMDD-HHMMSS>_<壳名>.md`**（**必须写全绝对路径**；题区内 `题目工作区/problem_2026C/04_决策/` 是误建目录——合并器只扫 `正式比赛/04_决策/`，写错即**永不合并**；壳名限 DSH/ZCode/Cursor/Human），主日志**只由** `python 正式比赛/tools/merge_decisions.py --apply` 写，合并后分片归档 `正式比赛/04_决策/merged/` 不删；主日志索引与瘦身用 `tools/dec_compact.py`。细则见 `04_决策/README.md`、`05_标准/对标融合条款集.md`（Q-22~Q-35）与 `2024C_坑到检验项映射表.md`；改 qc_gate 前后跑 `tools/tests/test_qc_gate.py`（15 用例回归）。
9. **子进程与节拍**：长进程切子进程（每段单一目标，≤2M 估算 token / ≤90 分钟；入口读交接单，出口过 qc-gate + 写小结；子进程之间只靠文件交接）。节拍自检四重触发（估算 token ≥200 万 / 工具调用 ≥40 次 / 墙钟 ≥45 分钟 / 工位边界），计数器 `tools/gauge.py`（start/auto/log/status/checkpoint，status 返回 1 即须自检）。
10. **夜间自治**：23:00–09:00 允许并推荐 nightshift 无人值守（队列 `.nightshift/`、晨报 `MORNING.md`）；本区**免走第 9 条节拍**，但**质量门禁不豁免**（算法/数值代码的 verify 须含 `qc_gate.py param-table`），产物落盘仍守第 2 条。flowkit 排除；白天有人值守的长任务走第 8/9 条。
11. **参数区**：涉及算法/数值的正式代码，可调参数集中在**表头参数区**（标记行含"参数区"三字），每条注明 `类型·单位 | 含义 | 调大→影响；调小→影响`；模块级常量不得散落在函数之间；交付前跑 `qc_gate.py param-table`（不过不得交付）。示范见 `05_标准/参数区示例.py`。
12. **Octave 可用**：直接调 `octave-cli`（实测 11.3.0，无需改 PATH）；`library/` 与题库的 `.m` 可直接跑。⚠️ `octave-cli --version` 走管道截断会显示 `[exit code: 1]`，那是管道提前关闭、**不是环境损坏**。
13. **上下文预算**：大文件**只 grep 不整读**；整读/检索前先看 `05_标准/上下文预算速查卡.md` 的**检索排除清单**（`99_归档/`、`04_文献/2026C/文献总表.*`、`分类清单/*_摘要.md`、`problem_2024C/01_data`）与占用率分区（≥50% 窗口即收尾换会话）；**Cursor/Grok 按 256k 计，≥40%（≈10.2 万 token）须写结论并提醒开新对话**（`05_标准/注意力机制区_Cursor-Grok.md`）；`05_标准/README.md` 是"读什么"清单（开场只读现行真源 3 份）。体检 `python 正式比赛/tools/ctx_audit.py`；决策日志瘦身 `python 正式比赛/tools/dec_compact.py --apply`。
14. **跨问口径继承**：从别问引用任何口径/机制/参数前，**先核它在题面里的归属**，并落《跨问引用核对表_Q<当前问>.md》（`02_analysis/`）——甲类**明示**（本问题面/模板表头/附录写了）｜乙类**参数借用**（本问只出现机制名词、参数唯一定义在别问题干；须附对标原文 + 写进论文假设表）｜丙类**无据一律"待定"，禁止写进定稿**。⚠️ 详解/规格书里的**编者概括**（如"每问在上一问基础上加一层机制"）**不是继承授权**。细则见《`AI_长任务流水线检验标准.md`》**Q-37**（现行版本见该文件头部版本行）。
15. **会话收尾（Q-38）**：进入工位的对话，每轮对人回复末尾给「下一步」+「进度表」；模板 `05_标准/对话收尾_Q38.md`，看板 `01_交接箱/进度板.md`。Cursor 规则 `.cursor/rules/03_对话收尾_进度表.mdc` 自动注入，不必再贴提示词。
16. **纪检与反思**：纪检只对**已完成**的过程做事后监督，目标=**减臃肿 + 减误判**，**绝不干扰在途工作**（在途 = mtime ≤30 分钟或属三壳本轮写权：只读、不动手、不抢写主日志）；不做 A/B/C、不删凭据（资格线/merged/archive/99_归档）。稽核器 `python 正式比赛/tools/discipline_audit.py --json`（只读）；报告落 `01_交接箱/纪检_*`；被推翻的结论、被修正的数字、被改的口径**当场登记** `05_标准/2026C_反思日志.md`（R 读取类 / M 建模类 → 检验项 → 迭代项），**入标才算闭环**。职权、红线、在途判定见 `05_标准/纪检委员会职权范围.md`。
17. **检验标准 v1.12（2026-09-13 新增 Q-49~Q-52）**：`Q-49` 闭环类约束默认＝交付口径（默认值/锚点不得静默失效，须窗口守卫＋末端硬门）｜`Q-50` 复核外部结论前先读其口径实现（自纠与作废分栏）｜`Q-51` 支撑材料包须与现役源一致且三者同代｜`Q-52` 参数换代＝同窗对照三向闭环（＋依赖件成套刷新＋旧件先备份后降级）；来源坑 R-58/R-59/M-41/M-42 见 `05_标准/2026C_反思日志.md`。
18. **数字订正（Q-53，v1.13）**：改一个数字后必须对该问 tex／摘要／表注 **grep 旧值**；合法残留须注明窗口／口径；结果节与检验节对同一量必须同数。细则见《`05_标准/AI_长任务流水线检验标准.md`》**Q-53**；来源坑 R-60。

